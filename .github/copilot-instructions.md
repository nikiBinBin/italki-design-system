Follow `AGENTS.md` at the repository root — it is the single instruction file for every agent here.

Before building any UI with this design system, run the intake gap scan described in `docs/INTAKE.md` and ask only the questions the request left open.
